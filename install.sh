#!/bin/sh
apt install rsync &&
apt install scrot &&
apt install python3-xlib &&
api install tesseract-ocr &&
api install tesseract-ocr-spa &&
pip3 install pillow &&
pip3 install pytesseract &&
pip3 install pyscreenshot &&
pip3 install pyautogui &&
pip3 install pyperclip &&
echo finish
